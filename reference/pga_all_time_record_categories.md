# List all-time record categories

List all-time record categories

## Usage

``` r
pga_all_time_record_categories(tour = "R")
```

## Arguments

- tour:

  Character. Tour code. Defaults to `"R"`.

## Value

A tibble of record IDs with category / subcategory labels.
